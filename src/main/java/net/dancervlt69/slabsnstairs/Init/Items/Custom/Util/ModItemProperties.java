package net.dancervlt69.slabsnstairs.Init.Items.Custom.Util;

import net.dancervlt69.slabsnstairs.Init.Items.ModItems;
import net.minecraft.client.renderer.item.ItemProperties;
import net.minecraft.resources.ResourceLocation;
import net.minecraft.world.item.Item;

public class ModItemProperties {
	/*public static void addCustomItemProperties() {
		makeModTrident(ModItems.MOD_TRIDENT.get());
	}

	public static void makeModTrident(Item pItem) {
		ItemProperties.register(pItem,
				new ResourceLocation("throwing"), (p_174585_, p_174586_, p_174587_, p_174588_) -> {
			return p_174587_ != null && p_174587_.isUsingItem() && p_174587_.getUseItem() == p_174585_ ? 1.0F : 0.0F;
		});
	} */
}

